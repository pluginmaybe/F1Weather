<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="F1Weather.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:F1Weather"
    xmlns:view="clr-namespace:F1Weather.View"
    Shell.FlyoutBehavior="Disabled"
    Title="F1Weather">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <!--<ShellContent
        Title="ChooseCircuit"
        ContentTemplate="{DataTemplate view:ChooseCircuit}"
        Route="ChooseCircuit" />-->
</Shell>